[package]
name = "solana_nft_minter"
version = "0.1.0"
description = "Solana NFT mint receipt program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "solana_nft_minter"

[features]
default = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []

[dependencies]
anchor-lang = "0.32.1"
